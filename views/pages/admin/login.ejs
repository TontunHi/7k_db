<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../../partials/head') %>
        <link rel="stylesheet" href="/css/pages/admin/login.css">
</head>

<body class="text-light">
    <div class="login-box shadow-lg">
        <h3 class="text-center text-warning mb-4">
            <i class="bi bi-shield-lock"></i> 7K Rebirth Admin
        </h3>

        <% if(error) { %>
            <div class="alert alert-danger py-2 small">
                <%= error %>
            </div>
            <% } %>

                <form action="/admin/login" method="POST">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <div class="mb-3">
                        <label class="form-label">Username</label>
                        <input type="text" name="username" class="form-control bg-dark text-light border-secondary"
                            required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Password</label>
                        <input type="password" name="password" class="form-control bg-dark text-light border-secondary"
                            required>
                    </div>
                    <button type="submit" class="btn btn-warning w-100 fw-bold">Login</button>
                </form>

                <div class="text-center mt-3">
                    <a href="/" class="text-secondary small text-decoration-none">← Back to Website</a>
                </div>
    </div>
</body>

</html>