<style>
    /* --- Navbar Styles --- */
    .navbar-modern {
        background: rgba(10, 10, 10, 0.85); /* สีดำโปร่งแสง */
        backdrop-filter: blur(12px); /* เบลอฉากหลัง */
        -webkit-backdrop-filter: blur(12px);
        border-bottom: 1px solid rgba(255, 255, 255, 0.08); /* เส้นขอบบางๆ */
        padding: 0.8rem 0;
        transition: all 0.3s ease;
    }

    /* Brand / Logo */
    .navbar-brand-text {
        font-weight: 800;
        letter-spacing: 1.5px;
        background: linear-gradient(45deg, #fff, #ccc);
        background-clip: text;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        font-size: 1.2rem;
        text-transform: uppercase;
    }

    /* Nav Links */
    .nav-link-custom {
        color: #b0b0b0 !important;
        font-weight: 500;
        font-size: 0.95rem;
        padding: 0.5rem 1rem !important;
        position: relative;
        transition: color 0.3s ease;
    }

    .nav-link-custom:hover {
        color: #fff !important;
    }

    /* Animated Underline Effect */
    .nav-link-custom::after {
        content: '';
        position: absolute;
        width: 0;
        height: 2px;
        bottom: 5px;
        left: 50%;
        background-color: #ffc107; /* สีทอง */
        transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
        transform: translateX(-50%);
        box-shadow: 0 0 8px rgba(255, 193, 7, 0.6);
    }

    .nav-link-custom:hover::after {
        width: 80%; /* ความยาวเส้นเมื่อ Hover */
    }

    /* Active State */
    .nav-link-custom.active {
        color: #ffc107 !important;
        text-shadow: 0 0 10px rgba(255, 193, 7, 0.3);
    }
    .nav-link-custom.active::after {
        width: 80%;
    }

    /* Dropdown Menu Animation */
    .dropdown-menu-custom {
        background-color: #1a1a1a;
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 12px;
        padding: 10px 0;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
        margin-top: 15px !important; /* เว้นระยะห่างจาก Navbar นิดหน่อย */
        
        /* Animation */
        animation: slideUpFade 0.3s ease forwards;
        transform-origin: top center;
    }

    @keyframes slideUpFade {
        from {
            opacity: 0;
            transform: translateY(10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Dropdown Items */
    .dropdown-item-custom {
        color: #ccc;
        padding: 8px 20px;
        font-size: 0.9rem;
        transition: all 0.2s;
        border-left: 2px solid transparent;
    }

    .dropdown-item-custom:hover {
        background-color: rgba(255, 255, 255, 0.05);
        color: #ffc107;
        border-left-color: #ffc107;
        padding-left: 25px; /* ขยับขวาเมื่อ Hover */
    }
    
    .dropdown-divider { border-top-color: rgba(255,255,255,0.1); }

    /* Mobile Toggler */
    .navbar-toggler {
        border: none;
        padding: 0;
    }
    .navbar-toggler:focus {
        box-shadow: none;
    }
</style>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top border-bottom border-secondary">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center gap-2" href="/">
            <img src="/images/about_website/logo_seven_knights_rebirth.png" alt="Logo" height="40">
            <span class="fw-bold text-warning">7K Rebirth</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                <li class="nav-item"><a class="nav-link" href="/tierlist">Tier List</a></li>
                
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        Codex
                    </a>
                    <ul class="dropdown-menu dropdown-menu-dark border-secondary">
                        <li><a class="dropdown-item" href="/codex/hero"><i class="bi bi-person-badge me-2"></i>Heroes</a></li>
                        <li><a class="dropdown-item" href="/codex/pet"><i class="bi bi-github me-2"></i>Pets</a></li>
                    </ul>
                </li>

                <li class="nav-item"><a class="nav-link" href="/build/legendary">Builds</a></li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">Guides</a>
                    <ul class="dropdown-menu dropdown-menu-dark border-secondary">
                        <li><a class="dropdown-item" href="/comp/stage">Stage Comp</a></li>
                        <li><a class="dropdown-item" href="/comp/dungeon">Dungeon Comp</a></li>
                        <li><a class="dropdown-item" href="/comp/guild-war">Guild War</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>

<style>
    @media (min-width: 992px) {
        .dropdown:hover .dropdown-menu {
            display: block;
            margin-top: 0;
            animation: fadeIn 0.3s;
        }
    }
    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(10px); }
        to { opacity: 1; transform: translateY(0); }
    }
    
    /* Active Link State Style (Optional) */
    .nav-link.active {
        color: #ffc107 !important;
        font-weight: bold;
    }
</style>